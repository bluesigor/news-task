import { Box } from "@mui/material";

const HomePage = () => {
  return (
    <Box
      sx={{
        flexGrow: 1,
        height: "74vh",
        p: 10,
      }}>
      HomePage
    </Box>
  );
};

export default HomePage;
